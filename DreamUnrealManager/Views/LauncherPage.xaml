<Page
    x:Class="DreamUnrealManager.Views.LauncherPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:DreamUnrealManager.Models"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:helpers="using:DreamUnrealManager.Helpers"
    mc:Ignorable="d">

    <!-- ========== 全局资源 ========== -->
    <Page.Resources>
        <!-- bool -> Visibility -->
        <helpers:BooleanToVisibilityConverter x:Key="BoolToVisibility" />

        <!-- ===== ItemsPanel（3种布局） ===== -->
        <!-- 网格：横向平铺、填满宽度、从顶部开始 -->
        <ItemsPanelTemplate x:Key="GridItemsPanel">
            <ItemsWrapGrid Orientation="Horizontal"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Top" />
        </ItemsPanelTemplate>

        <!-- 列表：单列整宽行（虚拟化，避免大量项目卡顿） -->
        <ItemsPanelTemplate x:Key="ListItemsPanel">
            <ItemsStackPanel Orientation="Vertical" />
        </ItemsPanelTemplate>

        <!-- 紧凑：单列整宽密集行（虚拟化） -->
        <ItemsPanelTemplate x:Key="CompactItemsPanel">
            <ItemsStackPanel Orientation="Vertical" />
        </ItemsPanelTemplate>

        <!-- ===== DataTemplates（3种卡片） ===== -->

        <MenuFlyout x:Key="ProjectMenuFlyoutTemplate">

            <MenuFlyoutItem Text="启动" Tag="{Binding}" Click="ContextMenu_LaunchProject_Click" />
            <MenuFlyoutItem Visibility="{Binding IsCPlusPlusProject, Mode=OneWay, Converter={StaticResource BoolToVisibility}}" Text="用 IDE 打开" Tag="{Binding}" Click="ContextMenu_OpenWithIDE_Click" />
            <MenuFlyoutItem Text="打开项目文件夹" Tag="{Binding}" Click="ContextMenu_OpenFolder_Click" />
            <MenuFlyoutItem Text="查看详情"
                            Tag="{Binding}"
                            Click="ContextMenu_ShowDetails_Click" />
            <MenuFlyoutItem Text="收藏/取消收藏"
                            Tag="{Binding}"
                            Click="ContextMenu_ToggleFavorite_Click" />
            <MenuFlyoutSeparator />
            <MenuFlyoutItem Visibility="{Binding IsCPlusPlusProject, Mode=OneWay, Converter={StaticResource BoolToVisibility}}"
                            Text="生成 VS 项目文件"
                            Tag="{Binding}"
                            Click="ContextMenu_GenerateVSProject_Click" />
            <MenuFlyoutItem Text="刷新项目" Tag="{Binding}" Click="ContextMenu_RefreshProject_Click" />
            <MenuFlyoutItem Text="切换引擎版本" Tag="{Binding}" Click="ContextMenu_SwitchEngine_Click" />
            <MenuFlyoutSeparator />
            <MenuFlyoutItem Text="从列表中移除" Tag="{Binding}" Click="ContextMenu_RemoveProject_Click" />
        </MenuFlyout>

        <!-- 网格卡片（图主导） -->
        <DataTemplate x:Key="GridProjectTemplate" x:DataType="models:ProjectInfo">
            <Border Width="360"
                    Height="360"
                    Margin="8"
                    CornerRadius="12"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    PointerEntered="ProjectCard_PointerEntered"
                    PointerExited="ProjectCard_PointerExited">

                <Grid>
                    <!-- 缩略图 -->
                    <Border CornerRadius="12">
                        <Image Source="{x:Bind ProjectIconPath}"
                               Stretch="UniformToFill"
                               ImageFailed="ProjectImage_ImageFailed" />
                    </Border>

                    <!-- 底部信息条 -->
                    <StackPanel Orientation="Vertical"
                                Spacing="2"
                                Padding="10"
                                VerticalAlignment="Bottom">
                        <StackPanel.Background>
                            <SolidColorBrush Color="Black" Opacity="0.45" />
                        </StackPanel.Background>
                        <TextBlock Text="{x:Bind DisplayName}"
                                   Style="{ThemeResource BodyStrongTextBlockStyle}"
                                   Foreground="White"
                                   TextTrimming="CharacterEllipsis" />
                        <TextBlock Text="{x:Bind EngineDisplayName, Mode=OneWay}"
                                   Style="{ThemeResource CaptionTextBlockStyle}"
                                   Foreground="#CCFFFFFF"
                                   TextTrimming="CharacterEllipsis" />
                    </StackPanel>

                    <!-- 悬浮操作层 -->
                    <Grid x:Name="HoverOverlay"
                          Background="#5F000000"
                          CornerRadius="12"
                          Visibility="Collapsed"
                          IsHitTestVisible="True">
                        <StackPanel Orientation="Horizontal"
                                    Spacing="8"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center">
                            <Button Content="启动"
                                    Tag="{x:Bind}"
                                    Click="StartProjectButton_OnClick" />
                            <Button Content="更多"
                                    Tag="{x:Bind}">
                                <Button.Flyout>
                                    <StaticResource ResourceKey="ProjectMenuFlyoutTemplate" />
                                </Button.Flyout>
                            </Button>
                        </StackPanel>
                    </Grid>

                    <!-- 元数据加载遮罩 -->
                    <Grid Background="#3F000000"
                          CornerRadius="12"
                          Visibility="{x:Bind IsLoadingMeta, Mode=OneWay, Converter={StaticResource BoolToVisibility}}">
                        <ProgressRing IsActive="True"
                                      Width="28" Height="28"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center" />
                    </Grid>

                    <Button Tag="{x:Bind}"
                            Click="ToggleFavorite_Click"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Margin="6"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="12"
                            Padding="6">
                        <FontIcon Glyph="{x:Bind FavoriteGlyph, Mode=OneWay}"
                                  FontSize="14" />
                    </Button>
                </Grid>

                <!-- 右键菜单（挂在 Border 上） -->
                <Border.ContextFlyout>
                    <StaticResource ResourceKey="ProjectMenuFlyoutTemplate" />
                </Border.ContextFlyout>
            </Border>
        </DataTemplate>
        
        <!-- 列表卡片（信息丰富） -->
        <DataTemplate x:Key="ListProjectTemplate" x:DataType="models:ProjectInfo">
            <Border Style="{StaticResource CardStyle}"
                    Padding="12"
                    MinHeight="140">

                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- 左：缩略图 -->
                    <Border Grid.Column="0"
                            Width="120" Height="120"
                            CornerRadius="6"
                            Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                        <Image Source="{x:Bind ProjectIconPath}"
                               Stretch="UniformToFill"
                               ImageFailed="ProjectImage_ImageFailed" />
                    </Border>

                    <!-- 中：文本信息 -->
                    <StackPanel Grid.Column="1"
                                Spacing="6"
                                VerticalAlignment="Center">

                        <!-- 标题行：名称 + Git 标签 -->
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock Text="{x:Bind DisplayName}"
                                       Style="{ThemeResource SubtitleTextBlockStyle}"
                                       VerticalAlignment="Center"
                                       TextTrimming="CharacterEllipsis" />
                            <Border Background="{ThemeResource AccentFillColorSecondaryBrush}"
                                    Padding="6,2"
                                    CornerRadius="6"
                                    VerticalAlignment="Center"
                                    Visibility="{x:Bind IsGitEnabled, Converter={StaticResource BoolToVisibility}}">
                                <TextBlock Text="Git"
                                           Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                                           Style="{ThemeResource CaptionTextBlockStyle}" />
                            </Border>
                            <Border Background="{ThemeResource AccentFillColorSecondaryBrush}"
                                    Padding="6,2"
                                    CornerRadius="6"
                                    VerticalAlignment="Center"
                                    Visibility="{x:Bind IsCPlusPlusProject, Converter={StaticResource BoolToVisibility}}">
                                <TextBlock Text="C++"
                                           Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                                           Style="{ThemeResource CaptionTextBlockStyle}" />
                            </Border>
                        </StackPanel>

                        <!-- 描述 -->
                        <TextBlock Text="{x:Bind DescriptionDisplay}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   TextTrimming="CharacterEllipsis" />

                        <!-- 元信息行 -->
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <FontIcon Glyph="&#xE7F8;" />
                                <TextBlock Text="{x:Bind EngineDisplayName}"
                                           Style="{ThemeResource CaptionTextBlockStyle}"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <FontIcon Glyph="&#xE823;" />
                                <TextBlock Text="{x:Bind LastModifiedString}"
                                           Style="{ThemeResource CaptionTextBlockStyle}"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <FontIcon Glyph="&#xE81E;" />
                                <TextBlock Text="{x:Bind ProjectSizeString}"
                                           Style="{ThemeResource CaptionTextBlockStyle}"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <FontIcon Glyph="&#xE8FA;" />
                                <TextBlock Text="{x:Bind ModulesCount}"
                                           Style="{ThemeResource CaptionTextBlockStyle}"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <TextBlock Text=" 模块"
                                           Style="{ThemeResource CaptionTextBlockStyle}"
                                           Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <FontIcon Glyph="&#xE90F;" />
                                <TextBlock Text="{x:Bind EnabledPluginsCount}"
                                           Style="{ThemeResource CaptionTextBlockStyle}"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <TextBlock Text=" 插件"
                                           Style="{ThemeResource CaptionTextBlockStyle}"
                                           Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                            </StackPanel>
                        </StackPanel>

                        <!-- Git 信息 -->
                        <TextBlock Text="{x:Bind GitInfoString}"
                                   Style="{ThemeResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                    </StackPanel>

                    <!-- 右：操作 -->
                    <StackPanel Grid.Column="2"
                                Orientation="Horizontal"
                                Spacing="8"
                                VerticalAlignment="Center">
                        <Button Tag="{x:Bind}"
                                Click="ToggleFavorite_Click"
                                Padding="4"
                                Background="Transparent"
                                BorderThickness="0"
                                ToolTipService.ToolTip="{x:Bind IsFavorite, Mode=OneWay}">
                            <FontIcon Glyph="{x:Bind FavoriteGlyph, Mode=OneWay}" />
                        </Button>
                        <Button MinWidth="110"
                                Tag="{x:Bind}"
                                Click="StartProjectButton_OnClick">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="&#xE768;" />
                                <TextBlock Text="启动项目" />
                            </StackPanel>
                        </Button>

                        <Button MinWidth="110"
                                Tag="{x:Bind}"
                                Click="StartWithIdeButton_OnClick"
                                Visibility="{x:Bind IsCPlusPlusProject, Mode=OneWay, Converter={StaticResource BoolToVisibility}}">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="&#xEC7A;" />
                                <TextBlock Text="{x:Bind GetIdeButtonText()}" />
                            </StackPanel>
                        </Button>

                        <Button MinWidth="110" Tag="{x:Bind}" ToolTipService.ToolTip="更多操作">
                            <Button.Flyout>
                                <StaticResource ResourceKey="ProjectMenuFlyoutTemplate" />
                            </Button.Flyout>
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="&#xE712;" />
                                <TextBlock Text="其他操作" />
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <!-- 加载遮罩（元数据阶段） -->
                    <Grid Grid.ColumnSpan="3"
                          Background="#3F000000"
                          Visibility="{x:Bind IsLoadingMeta, Mode=OneWay, Converter={StaticResource BoolToVisibility}}"
                          CornerRadius="8">
                        <ProgressRing IsActive="True"
                                      Width="28" Height="28"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center" />
                    </Grid>
                </Grid>
                <!-- 右键菜单（挂在根） -->
                <Border.ContextFlyout>
                    <StaticResource ResourceKey="ProjectMenuFlyoutTemplate" />
                </Border.ContextFlyout>
            </Border>
        </DataTemplate>

        <!-- 紧凑卡片（高密度） -->
        <DataTemplate x:Key="CompactProjectTemplate" x:DataType="models:ProjectInfo">
            <Border Style="{StaticResource CardStyle}"
                    Padding="0">
                <Grid Height="92"
                      Padding="10,8"
                      ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- 缩略图 -->
                    <Border Grid.Column="0"
                            Width="76"
                            Height="76"
                            CornerRadius="6"
                            Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                        <Grid>
                            <Image Width="76"
                                   Height="76"
                                   Stretch="UniformToFill"
                                   Source="{Binding ThumbnailUri}"
                                   ImageFailed="ProjectImage_ImageFailed"
                                   AutomationProperties.Name="{Binding DisplayName}" />

                            <Grid Background="#55000000"
                                  Visibility="{Binding IsLoadingMeta, Converter={StaticResource BoolToVisibility}}">
                                <ProgressRing IsActive="True"
                                              Width="20"
                                              Height="20"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                            </Grid>
                        </Grid>
                    </Border>

                    <!-- 文本区 -->
                    <Grid Grid.Column="1"
                          VerticalAlignment="Center"
                          RowSpacing="4">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{Binding DisplayName}"
                                       FontSize="16"
                                       FontWeight="SemiBold"
                                       TextTrimming="CharacterEllipsis"
                                       VerticalAlignment="Center"
                                       ToolTipService.ToolTip="{Binding DisplayName}" />

                            <StackPanel Grid.Column="1"
                                        Orientation="Horizontal"
                                        Spacing="6"
                                        VerticalAlignment="Center">
                                <Border Background="{ThemeResource AccentFillColorSecondaryBrush}"
                                        Padding="6,2"
                                        CornerRadius="6"
                                        Visibility="{Binding IsGitEnabled, Converter={StaticResource BoolToVisibility}}">
                                    <TextBlock Text="Git"
                                               Style="{ThemeResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}" />
                                </Border>
                                <Border Background="{ThemeResource AccentFillColorSecondaryBrush}"
                                        Padding="6,2"
                                        CornerRadius="6"
                                        Visibility="{Binding IsCPlusPlusProject, Converter={StaticResource BoolToVisibility}}">
                                    <TextBlock Text="C++"
                                               Style="{ThemeResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}" />
                                </Border>
                            </StackPanel>
                        </Grid>

                        <StackPanel Grid.Row="1"
                                    Orientation="Horizontal"
                                    Spacing="6"
                                    VerticalAlignment="Center">
                            <FontIcon Glyph="&#xE7F8;"
                                      FontSize="12"
                                      Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                            <TextBlock Text="{Binding EngineDisplayName}"
                                       Style="{ThemeResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       TextTrimming="CharacterEllipsis" />
                        </StackPanel>

                        <StackPanel Grid.Row="2"
                                    Orientation="Horizontal"
                                    Spacing="12"
                                    VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <FontIcon Glyph="&#xE823;"
                                          FontSize="11"
                                          Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                                <TextBlock Text="{Binding LastModifiedString}"
                                           Style="{ThemeResource CaptionTextBlockStyle}"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <FontIcon Glyph="&#xE81E;"
                                          FontSize="11"
                                          Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                                <TextBlock Text="{Binding ProjectSizeString}"
                                           Style="{ThemeResource CaptionTextBlockStyle}"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            </StackPanel>
                        </StackPanel>
                    </Grid>

                    <!-- 操作区 -->
                    <StackPanel Grid.Column="2"
                                Orientation="Horizontal"
                                Spacing="6"
                                VerticalAlignment="Center">
                        <Button Tag="{Binding}"
                                Click="ToggleFavorite_Click"
                                Padding="8,6"
                                MinWidth="36"
                                ToolTipService.ToolTip="收藏/取消收藏">
                            <FontIcon Glyph="{Binding FavoriteGlyph}" />
                        </Button>

                        <Button Content="启动"
                                MinWidth="84"
                                Tag="{Binding}"
                                Click="StartProjectButton_OnClick" />

                        <Button MinWidth="110"
                                Content="用 IDE 打开"
                                Tag="{Binding}"
                                Click="StartWithIdeButton_OnClick"
                                Visibility="{Binding IsCPlusPlusProject, Converter={StaticResource BoolToVisibility}}" />

                        <Button Tag="{Binding}"
                                MinWidth="40"
                                Click="MoreCommandsButton_OnClick"
                                Flyout="{StaticResource ProjectMenuFlyoutTemplate}"
                                ToolTipService.ToolTip="更多操作">
                            <FontIcon Glyph="&#xE712;" />
                        </Button>
                    </StackPanel>

                </Grid>
                <Border.ContextFlyout>
                    <StaticResource ResourceKey="ProjectMenuFlyoutTemplate" />
                </Border.ContextFlyout>
            </Border>
        </DataTemplate>


        <!-- ListViewItem 容器样式 -->
        <Style x:Key="ProjectItemContainerStyle" TargetType="ListViewItem">
            <Setter Property="Margin" Value="4" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
            <Setter Property="Background" Value="Transparent" />
        </Style>

    </Page.Resources>

    <!-- ========== 页面布局 ========== -->
    <Grid x:Name="ContentArea">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />   <!-- 标题 -->
            <RowDefinition Height="Auto" />   <!-- 工具栏 -->
            <RowDefinition Height="Auto" />   <!-- 筛选栏 -->
            <RowDefinition Height="*" />      <!-- 列表 -->
            <RowDefinition Height="Auto" />   <!-- 状态栏 -->
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <StackPanel Grid.Row="0" Margin="24,24,24,4" Spacing="4">
            <TextBlock Text="项目启动器" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock Text="选择并启动你的 Unreal Engine 项目"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <!-- 工具栏 -->
        <Border Grid.Row="1"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="0,1,0,1"
                Padding="24,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- 搜索 -->
                <AutoSuggestBox x:Name="ProjectSearchBox"
                                PlaceholderText="搜索项目..."
                                HorizontalAlignment="Stretch"
                                MaxWidth="360"
                                VerticalAlignment="Center"
                                QueryIcon="Find"
                                TextChanged="ProjectSearchBox_TextChanged" />

                <!-- 右侧按钮 + 布局切换 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <controls:Segmented x:Name="LayoutSegmented"
                                        SelectedIndex="0"
                                        SelectionChanged="LayoutSegmented_SelectionChanged">
                        <controls:SegmentedItem>
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="&#xE80A;" />
                                <TextBlock Text="网格" />
                            </StackPanel>
                        </controls:SegmentedItem>
                        <controls:SegmentedItem>
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="&#xE8FD;" />
                                <TextBlock Text="列表" />
                            </StackPanel>
                        </controls:SegmentedItem>
                        <controls:SegmentedItem>
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="&#xE8FD;" />
                                <TextBlock Text="紧凑" />
                            </StackPanel>
                        </controls:SegmentedItem>
                    </controls:Segmented>

                    <!-- 分隔线：区分布局切换与命令按钮 -->
                    <Border Width="1"
                            Margin="4,6"
                            Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

                    <Button Style="{StaticResource AccentButtonStyle}" Click="AddProject_Click">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE710;" FontSize="16" />
                            <TextBlock Text="添加项目" />
                        </StackPanel>
                    </Button>
                    <Button Click="AutoSearchProjects_Click">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE721;" FontSize="16" />
                            <TextBlock Text="自动搜索" />
                        </StackPanel>
                    </Button>
                    <Button Click="RefreshProjects_Click">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE72C;" FontSize="16" />
                            <TextBlock Text="刷新列表" />
                        </StackPanel>
                    </Button>
                    <Button Click="OpenProjectFolder_Click">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xED25;" FontSize="16" />
                            <TextBlock Text="打开项目文件夹" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 筛选栏 -->
        <Border Grid.Row="2"
                Background="{ThemeResource LayerFillColorDefaultBrush}"
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                BorderThickness="0,0,0,1"
                Padding="24,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- 左侧：筛选与排序控件 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="12" VerticalAlignment="Bottom">
                    <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center" Margin="0,0,0,4">
                        <FontIcon Glyph="&#xE71C;" FontSize="16" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock Text="筛选:" FontWeight="SemiBold" VerticalAlignment="Center" />
                    </StackPanel>

                    <ComboBox x:Name="EngineFilterComboBox"
                              PlaceholderText="选择引擎版本"
                              MinWidth="180"
                              SelectionChanged="EngineFilterComboBox_SelectionChanged">
                        <ComboBox.Header>
                            <TextBlock Text="引擎版本"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </ComboBox.Header>
                    </ComboBox>

                    <ComboBox x:Name="SortOrderComboBox"
                              MinWidth="140"
                              SelectionChanged="SortOrderComboBox_SelectionChanged">
                        <ComboBox.Header>
                            <TextBlock Text="排序方式"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </ComboBox.Header>
                        <ComboBoxItem Content="最近使用" Tag="LastUsed" IsSelected="True" />
                        <ComboBoxItem Content="名称" Tag="Name" />
                        <ComboBoxItem Content="引擎版本" Tag="Engine" />
                        <ComboBoxItem Content="项目大小" Tag="Size" />
                        <ComboBoxItem Content="修改时间" Tag="Modified" />
                    </ComboBox>

                    <ToggleButton x:Name="FavoriteOnlyToggle"
                                  Content="只看收藏"
                                  Checked="FavoriteOnlyToggle_Checked"
                                  Unchecked="FavoriteOnlyToggle_Checked"
                                  VerticalAlignment="Center"
                                  Margin="0,0,0,4" />

                    <ToggleButton x:Name="FavoriteFrontToggle"
                                  Content="收藏在前"
                                  Checked="FavoriteFrontToggle_Checked"
                                  Unchecked="FavoriteFrontToggle_Checked"
                                  VerticalAlignment="Center"
                                  Margin="0,0,0,4" />
                </StackPanel>

                <!-- 右侧：统计摘要 + 清除操作 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="12" VerticalAlignment="Bottom" Margin="16,0,0,4">
                    <TextBlock x:Name="FilterStatsText"
                               Text="显示 0 个项目"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               VerticalAlignment="Center" />
                    <Button Content="清除筛选"
                            Click="ClearFilters_Click" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- 内容区 -->

        <Grid Grid.Row="3" Margin="24,0,24,12" CornerRadius="0,0,8,8">
            
            <!-- 空状态 -->
            <StackPanel x:Name="EmptyStatePanel"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Spacing="20"
                        Visibility="Collapsed">
                <FontIcon Glyph="&#xE7C3;" FontSize="64"
                          Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                          HorizontalAlignment="Center" />
                <StackPanel Spacing="8" HorizontalAlignment="Center">
                    <TextBlock x:Name="EmptyStateTitle"
                               Text="暂无项目"
                               Style="{StaticResource SubtitleTextBlockStyle}"
                               HorizontalAlignment="Center"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <TextBlock x:Name="EmptyStateMessage"
                               Text="点击下方按钮来添加你的第一个 Unreal Engine 项目"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               HorizontalAlignment="Center"
                               TextAlignment="Center"
                               MaxWidth="400"
                               TextWrapping="Wrap" />
                </StackPanel>
                <Button HorizontalAlignment="Center" MinWidth="120" Padding="20,12"
                        Style="{StaticResource AccentButtonStyle}" Click="AddProject_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE710;" FontSize="16" />
                        <TextBlock Text="添加项目" />
                    </StackPanel>
                </Button>
            </StackPanel>

            <!-- 列表 -->
            <ListView x:Name="ProjectsListView"
                      ItemsSource="{x:Bind FilteredProjects, Mode=OneWay}"
                      ItemContainerStyle="{StaticResource ProjectItemContainerStyle}"
                      IsItemClickEnabled="False"
                      SelectionMode="None"
                      HorizontalContentAlignment="Stretch"
                      VerticalContentAlignment="Top"
                      ScrollViewer.HorizontalScrollMode="Disabled"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      ItemsPanel="{StaticResource GridItemsPanel}"
                      ItemTemplate="{StaticResource GridProjectTemplate}" >
            </ListView>
            
            <Grid x:Name="GlobalOverlay"
                  Background="#80000000"
                  CornerRadius="0,0,8,8"
                  Visibility="Collapsed">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="12">
                    <ProgressRing IsActive="True" Width="40" Height="40" />
                    <TextBlock x:Name="GlobalOverlayText"
                               Text="正在加载项目信息..."
                               Foreground="White"
                               FontSize="16"
                               TextAlignment="Center" />
                </StackPanel>
            </Grid>
        </Grid>


        <!-- 底部状态条 -->
        <Border Grid.Row="4"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="0,1,0,0"
                Padding="24,8"
                VerticalAlignment="Bottom">
            <TextBlock x:Name="StatusText"
                       Text="就绪"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </Border>

    </Grid>
</Page>
